[defaults]
host_key_checking = False
inventory = inventory.yml
remote_user = andrew  # пользователь
become = true
become_method = sudo
stdout_callback = yaml
callback_whitelist = profile_tasks

[ssh_connection]
pipelining = True
control_path = ~/.ansible/cp/%%h-%%r