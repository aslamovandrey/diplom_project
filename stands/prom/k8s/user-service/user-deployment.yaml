apiVersion: apps/v1
kind: Deployment

metadata:
  name: user-service

spec:

  replicas: 1

  selector:
    matchLabels:
      app: user-service

  template:

    metadata:
      labels:
        app: user-service

    spec:

      containers:

      - name: user-service

        image: cr.yandex/crpptbfa7s6gcd28ucld/user-service:1.1

        ports:

        - containerPort: 5001

        env:

        - name: DB_HOST
          value: "10.1.0.11"

        - name: DB_PORT
          value: "5432"

        - name: DB_NAME
          value: "ajax"

        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: DB_USER

        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: DB_PASSWORD