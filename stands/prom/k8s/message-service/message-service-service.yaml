apiVersion: v1
kind: Service

metadata:
  name: message-service
  namespace: messenger-ajax

spec:
  selector:
    app: message-service

  ports:
    - port: 80
      targetPort: 5002

  type: ClusterIP