all:

  vars:

    ansible_user: andrew

    ansible_ssh_private_key_file: ~/.ssh/id_rsa

    ansible_ssh_common_args: >-
      -o ProxyJump=andrew@158.160.230.164

  children:
    postgres:
      hosts:
        db01:
          ansible_host: 10.1.0.11

    jenkins:
      hosts:
        jenkins-server:
          ansible_host: 10.3.0.13