all:
  children:
    postgres_cluster:
      hosts:
        pg-node1:
          ansible_host: 93.77.185.109      # IP первой ВМ
          node_name: postgres-master
          node_ip: 10.1.0.16
          node_id: 1
        pg-node2:
          ansible_host: 81.26.189.86      # IP второй ВМ
          node_name: postgres-replica
          node_ip: 10.2.0.14
          node_id: 2
      vars:
        cluster_name: main_cluster
        patroni_scope: "main_cluster"
        postgresql_version: 16
        patroni_version: "3.4.0"
        etcd_initial_cluster: "node1=https://{{ hostvars['pg-node1']['node_ip'] }}:2380,node2=https://{{ hostvars['pg-node2']['node_ip'] }}:2380"